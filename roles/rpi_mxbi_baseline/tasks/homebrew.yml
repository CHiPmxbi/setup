---
- name: Check Linuxbrew
  ansible.builtin.stat:
    path: "{{ rpi_mxbi_baseline_homebrew_prefix }}/bin/brew"
  register: rpi_mxbi_baseline_homebrew_binary

- name: Download the Homebrew installer
  ansible.builtin.get_url:
    url: https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh
    dest: /tmp/mxbi-homebrew-install.sh
    mode: '0755'
  when: not rpi_mxbi_baseline_homebrew_binary.stat.exists

- name: Create the Linuxbrew installation directory
  become: true
  ansible.builtin.file:
    path: "{{ rpi_mxbi_baseline_homebrew_prefix }}"
    state: directory
    owner: "{{ rpi_mxbi_baseline_user }}"
    group: "{{ rpi_mxbi_baseline_group }}"
    mode: '0755'
  when: not rpi_mxbi_baseline_homebrew_binary.stat.exists

- name: Install Linuxbrew
  become: true
  become_user: "{{ rpi_mxbi_baseline_user }}"
  ansible.builtin.command:
    cmd: /bin/bash /tmp/mxbi-homebrew-install.sh
    creates: "{{ rpi_mxbi_baseline_homebrew_prefix }}/bin/brew"
  environment:
    HOME: "{{ rpi_mxbi_baseline_home }}"
    NONINTERACTIVE: '1'
    HOMEBREW_NO_ANALYTICS: '1'

- name: Install Homebrew packages
  become: true
  become_user: "{{ rpi_mxbi_baseline_user }}"
  community.general.homebrew:
    name: "{{ rpi_mxbi_baseline_homebrew_packages }}"
    path: "{{ rpi_mxbi_baseline_homebrew_prefix }}/bin"
    state: latest
    update_homebrew: true
  environment:
    HOME: "{{ rpi_mxbi_baseline_home }}"
    HOMEBREW_NO_AUTO_UPDATE: '1'
  when: rpi_mxbi_baseline_homebrew_binary.stat.exists or not ansible_check_mode

- name: Create the user font directory
  become: true
  ansible.builtin.file:
    path: "{{ rpi_mxbi_baseline_home }}/.local/share/fonts/MapleMono-NF-CN"
    state: directory
    owner: "{{ rpi_mxbi_baseline_user }}"
    group: "{{ rpi_mxbi_baseline_group }}"
    mode: '0755'

- name: Check whether the Maple Mono font is installed
  ansible.builtin.stat:
    path: "{{ rpi_mxbi_baseline_home }}/.local/share/fonts/MapleMono-NF-CN/MapleMono-NF-CN-Regular.ttf"
  register: rpi_mxbi_baseline_maple_font_file

- name: Download the Maple Mono font archive
  ansible.builtin.get_url:
    url: >-
      https://github.com/subframe7536/maple-font/releases/download/v{{ rpi_mxbi_baseline_maple_font_version
      }}/MapleMono-NF-CN-unhinted.zip
    dest: "/tmp/MapleMono-NF-CN-{{ rpi_mxbi_baseline_maple_font_version }}.zip"
    checksum: "{{ rpi_mxbi_baseline_maple_font_checksum }}"
    mode: '0644'
  when: not rpi_mxbi_baseline_maple_font_file.stat.exists

- name: Install the Maple Mono font
  become: true
  become_user: "{{ rpi_mxbi_baseline_user }}"
  ansible.builtin.unarchive:
    src: "/tmp/MapleMono-NF-CN-{{ rpi_mxbi_baseline_maple_font_version }}.zip"
    dest: "{{ rpi_mxbi_baseline_home }}/.local/share/fonts/MapleMono-NF-CN"
    remote_src: true
    creates: "{{ rpi_mxbi_baseline_home }}/.local/share/fonts/MapleMono-NF-CN/MapleMono-NF-CN-Regular.ttf"
  notify: Refresh the user font cache
  when: not ansible_check_mode
