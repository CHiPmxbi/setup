# Repository Notes

- Treat `rpi-mxbi-baseline.yml` as the shared-workstation provisioning entrypoint.
- Treat `experiment.yml` as the experiment deployment entrypoint.
- The repository is a control-machine setup project and must not be cloned onto managed devices as part of provisioning.
